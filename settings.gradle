dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this repository is going to shut down soon
        repositories {
            //adding the jitpack
            maven { url 'https://jitpack.io' }
        }
    }
}
rootProject.name = "Usell Up"
include ':app'
